import './bootstrap';     // carrega axios, Popper e configurações do Laravel
import 'bootstrap';       // carrega o JavaScript do Bootstrap (depende de Popper)

import Alpine from 'alpinejs';  
window.Alpine = Alpine;  
Alpine.start();
